{{!< default}}
{{#if tag.feature_image}}
<figure class="header-cover" style="background-image: url('{{img_url tag.feature_image}}');"></figure>
{{else}}
{{#if @blog.cover_image}}
<figure class="header-cover" style="background-image: url('{{@blog.cover_image}}');"></figure>
{{/if}}
{{/if}}
{{#tag}}
<header class="tag-header">
  <h1>
    &lt;{{name}}&gt;
    <small>
      {{#if description}}
      {{description}}
      {{else}}
      Total: {{../pagination.total}}
      {{/if}}
    </small>
  </h1>
</header>
{{/tag}}
{{> "post_list"}}
